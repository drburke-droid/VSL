# VSL

Visual Systems Lab retro CRT front page. The site lives in [`vsl-retro/`](vsl-retro/)
and is published to GitHub Pages from `main` by `.github/workflows/pages.yml`.

Run locally:

```bash
cd vsl-retro
python3 -m http.server 8000
```
