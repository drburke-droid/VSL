# Handoff: Calgary Vision Centre retro homepage + Visual Systems Lab

Status as of 2026-09-25 (commit after `8c448e7` on `main`). Read `CLAUDE.md` and `README.md` first;
this file is the "where are we and what's next" summary.

## What is live

GitHub Pages publishes `vsl-retro/` from `main` via `.github/workflows/pages.yml`
(push to main → live in ~30–60 s; browsers cache for 10 min).

| URL | What it is |
|---|---|
| https://drburke-droid.github.io/VSL/ | Visual Systems Lab (13 tools) on the CRT. Target: calgaryvisioncentre.com/lab |
| https://drburke-droid.github.io/VSL/home.html | Homepage "then": 1998 web on the CRT (landscape) or a PDA (portrait) |
| https://drburke-droid.github.io/VSL/today.html | Homepage "now": Momento360 clinic tour on an angled tablet (landscape) or a straight-on phone (portrait) |
| https://drburke-droid.github.io/VSL/sites/cvc1998/ | Our 1998 site, re-created |

Squarespace test page with the embed: https://calgaryvisioncentre.com/home-1 (a copy of the homepage).

## The embed

Copy from `embed/home-pair.html` (Raw button on GitHub). **Never copy long lines out of the
terminal**: characters get dropped at wraps. The file is kept to short lines for that reason.

Desktop settings the owner tuned and approved (keep them):
monitor `width: 80%; margin-bottom: 4%`, tablet `width: 85%; margin-bottom: 10%`, columns `1fr 1fr`.
Mobile (`max-width: 760px`): stacked; retro frame 328/613; tour frame 708/1418 (phone), `width: 90%; max-width: 420px`.

Frame URLs carry `?v=N`. Bump N in the Squarespace block when a change must show immediately.

## Architecture in one breath

- `index.html` / `home.html` share `css/style.css` and `js/app.js`. `<body data-tools>` picks the
  JSON (`js/tools.json` lab, `js/home.json` homepage); `data-idle` sets the screensaver delay.
  `home.html` is `index.html` with different copy; chrome changes go in both.
- Scene switch is by **orientation** (portrait → handheld/phone), not width, so two side-by-side
  frames on desktop keep the CRT/tablet.
- CRT: `assets/monitor.webp` (1157×1129, alpha), stage 738×720, screen box `left:68 top:68 601×426`
  clipped by a `clip-path` to the bezel's bulge. Desktop box 645×457 at scale 0.9318.
  **No displacement/barrel filter** — it broke 1px lines and clipped text; do not reintroduce.
- Win95-style chrome (Tahoma, 12 px; README 14 px), icons on the teal desktop (`#desk`), taskbar
  with Start menu (Minesweeper, Solitaire, window arrange, degauss, screensaver, scanlines, About,
  Shut Down), bouncing-CVC-logo screensaver (corner hit shatters it).
- `today.html` is standalone (no app.js). Tablet: tour mapped with `matrix3d` onto the screen's
  four corners, masked by `assets/ipad-mask.png` (silhouette in the **alpha** channel — CSS masks
  read alpha). Phone: plain rectangle behind `assets/phone.webp`, camera island kept in front.
  One tour iframe moves between scenes. Both images were generated by a Python build script in
  the session scratchpad (not in the repo); regenerating needs the source images in Downloads:
  tablet `ChatGPT Image Sep 25, 2026, 08_39_29 AM.png`, phone `ChatGPT Image Sep 25, 2026, 01_44_55 PM.png`.

## Content rules (owner's, non-negotiable)

- Homepage copy: no individual's name; no superiority/comparative words (best, latest, advanced,
  leading, most, superior). College of Optometrists advertising rules. The lab page may credit Dr. Burke.
- Homepage programs are **1998 only** (another year ruins the joke), each archive capture checked
  by eye for a clean render. Currently 16: Google, AltaVista, Netscape, Amazon*, Nasdaq, Apple*,
  NHL/NFL standings, NHL.com, ESPN, NBA, MLB, Calgary Herald, BBC News*, Winamp, Our Site 1998.
  (* = local screenshots in `sites/`, links don't click.)
- No link on the homepage may load calgaryvisioncentre.com inside the frame (it would nest the
  site once this is the homepage). "Our Site 2026" and the tour program were removed for this.

## Known issues / open items

1. **Tablet drag on /home-1**: an empty Squarespace block sat over the tablet and swallowed clicks.
   Owner was told to delete it; not yet confirmed fixed. Diagnose with `document.elementFromPoint`
   at the frame's centre on the live page.
2. **Real-device test of the phone scene** (touch drag on iPhone) has not been done.
3. Copy the owner is using on the page (Squarespace, not in this repo): heading along the lines of
   "Computers have come a long way since 1998. So has eye care." and a paragraph beginning
   "The screen above shows the web as it looked in 1998…".
4. The Wayback Machine is a third party; if a capture degrades, re-screen and swap it in `js/home.json`.

## Working habits that paid off

- Solitaire and Minesweeper redraw on every click, so the browser's `dblclick` never fires (the
  second click lands on a new element). Detect double-clicks by time + card inside the click handler.
  Test games with real `page.mouse` clicks, not `el.click()`, or bugs like this hide.

- Verify in Chrome before every push (a local `python -m http.server 8765`); stop the server by
  port, never by killing all python.
- Chrome screenshots of pages running the 360 tour often time out; composite images offline instead.
- Another session committed to main via PRs mid-project. Always `git fetch` before pushing and
  rebase, keeping the newer decisions.
